{{! shoppingDetailView.hbs }}
 
{{#extends "layout"}}
 
    {{#block "scripts"}}
    {{/block}}
 
    {{#block "content" mode="append"}}

        <h3>
            Shopping List: {{purchase_date}}
        </h3>

        {{> shoppingView-table}}

        {{#*inline "shoppingData-table"}}
            <table class="u-full-width">
                <thead>
                    <tr>
                        <!-- <th>product_id</th> -->
                        <th>Product</th>
                        <th>Category</th>
                        <th>Quantity On Hand</th>
                        <th>Unit</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                            <form>
                                <!-- <td><input type="number" value="{{product_id}}" id="f{{product_id}}"></td> -->
                                <td><input type="text" value="{{product_name}}" id="{{product_name}}"></td>
                                <select name="product_category"> 
                                    <option disabled selected value>select location</option>
                                    {{#each product_category}}
                                    <option value="{{product_category}}">{{product_category}}</option>
                                    {{/each}}
                                </select>
                                <!-- <td><input type="number" value="{{location_id}}" id="f{{location_id}}"></td> -->
                                <td><input type="number" value="{{stored_quantity}}" id="{{stored_quantity}}"></td>
                                <select name="unit">
                                    <option disabled selected value>select unit of measure</option>
                                    {{#each unit}}
                                    <option value="{{unit}}">{{unit}}</option>
                                    {{/each}}
                                </select>
                                <td><input type="submit" value="📝" onclick="" action="/update"></td>
                                <td><input type="submit" value="❌" onclick="" action="/delete"></td>
                            </form>
                        </tr>
                    <!-- FUTURE IMPLEMENTATION, SORT BY CATEGORY AND SEPARATE BY CATEGORY NAME -->
                    {{#each products}}
                        <tr>
                            <form>
                                <!-- <td>{{product_id}}</td> -->
                                <td><input type="text" value="{{product_name}}" id="{{product_name}}"></td>
                                <select name="product_category"> 
                                    <option disabled selected value>select location</option>
                                    {{#each product_category}}
                                    <option value="{{product_category}}">{{product_category}}</option>
                                    {{/each}}
                                    <!-- NEED TO VALIDATE THE SELECTED VALUE INTERACTION WITH QUERY FUNCTION AND HANDLEBARS -->
                                    <option value="{{product_category}} "selected="selected">{{product_category}}</option>
                                </select>
                                <!-- ARBITRARILY SET AS STORED QUANTITY TO HAVE DATA. 
                                     NEEDS CUST UI FUNC TO GEN DATA. MIGHT NEED SOME 
                                     OTHER METHOD FOR COURSE-->
                                <td><input type="number" value="{{stored_quantity}}" id="{{stored_quantity}}"></td>
                                <select name="unit">
                                    {{#each unit}}
                                    <option value="{{unit}}">{{unit}}</option>
                                    {{/each}}
                                    <!-- NEED TO VALIDATE THE SELECTED VALUE INTERACTION WITH QUERY FUNCTION AND HANDLEBARS -->
                                    <option value="{{unit}}" selected="selected">{{unit}}</option>
                                </select>
                                <td><input type="submit" value="📝" onclick="updateListProduct" action="/update"></td>
                                <td><input type="submit" value="❌" onclick="deleteListProduct" action="/delete"></td>
                            </form>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        {{/inline}}

        {{> shoppingData-table}}


        <!-- Always wrap checkbox and radio inputs in a label and use a <span class="label-body"> inside of it -->

        <!-- Note: The class .u-full-width is just a utility class shorthand for width: 100% -->
    {{/block}}
 
{{/extends}}